const _defaultState = [];

const CommentErrorsReducer = (oldState = _defaultState, action) => {
  switch (action.type) {

    default:
      return oldState;

  }
};

export default CommentErrorsReducer;
